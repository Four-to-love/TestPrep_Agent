# Project Rules

- **Execution Constraint**: Do not run any terminal commands or scripts. Always instruct the user to run them manually.
- **Styling Constraint**: All styling requests must be implemented/planned using only Streamlit's native theme properties in `.streamlit/config.toml` (no custom CSS or HTML in Python code).
